import React from 'react';
import { Box, Typography } from '@mui/material';

const OSReports: React.FC = () => {
  return (
    <Box>
      <Typography variant="h5">Relatórios de Ordem de Serviço</Typography>
      <Typography variant="body1" color="text.secondary">
        Conteúdo para relatórios de ordem de serviço.
      </Typography>
    </Box>
  );
};

export default OSReports;