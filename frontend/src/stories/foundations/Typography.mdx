import { Meta, Typeset } from '@storybook/blocks';
import { typography } from '../../styles/designTokens';

<Meta title="Design System/Foundations/Typography" />

# Typography

Nossa escala tipográfica promove clareza e hierarquia no conteúdo.

<Typeset
  fontSizes={[
    Number(typography.displayLarge.fontSize.replace('px', '')),
    Number(typography.headlineSmall.fontSize.replace('px', '')),
    Number(typography.titleLarge.fontSize.replace('px', '')),
    Number(typography.bodyLarge.fontSize.replace('px', '')),
    Number(typography.labelSmall.fontSize.replace('px', '')),
  ]}
  fontWeight={typography.bodyLarge.fontWeight}
  sampleText="O rápido gambá marrom salta sobre o cão preguiçoso"
  fontFamily={typography.fontFamilyPrimary}
/>
