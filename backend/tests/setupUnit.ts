// Setup for Unit Tests
// Currently empty as unit tests should mock their own dependencies locally.
// If global mocks are needed for all unit tests, add them here.
import { vi } from 'vitest';

// Example: Global environment variables
process.env.JWT_SECRET = 'unit-test-secret';
