import { Router } from 'express';

const router = Router();

// TODO: As funções do controller para estas rotas não foram encontradas. Comentado para permitir o build.
// // Rotas para diagnostic_nodes
// router.post('/diagnostic-nodes', diagnosticNodesController.createDiagnosticNode);
// router.get('/diagnostic-nodes', diagnosticNodesController.getDiagnosticNodes);
// router.get('/diagnostic-nodes/:id', diagnosticNodesController.getDiagnosticNodeById);
// router.put('/diagnostic-nodes/:id', diagnosticNodesController.updateDiagnosticNode);
// router.delete('/diagnostic-nodes/:id', diagnosticNodesController.deleteDiagnosticNode);

// // Rotas para diagnostic_node_options
// router.post('/diagnostic-node-options', diagnosticNodesController.createDiagnosticNodeOption);
// router.get('/diagnostic-node-options', diagnosticNodesController.getDiagnosticNodeOptions);
// router.put('/diagnostic-node-options/:id', diagnosticNodesController.updateDiagnosticNodeOption);
// router.delete('/diagnostic-node-options/:id', diagnosticNodesController.deleteDiagnosticNodeOption);

export default router;
